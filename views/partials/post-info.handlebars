        <article class="post">
            <div class="title">
                <h2>{{title}}</h2><span>{{user.username}} on {{created_at}}</span>

            </div>
            <div class="meta">
                {{post_text}}

                |
                <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
            </div>
        </article>